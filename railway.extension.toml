[build]
buildCommand = "pnpm --filter ./apps/extension run build"

[deploy]
startCommand = "sh -lc \"echo extension artifact build service; sleep infinity\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
